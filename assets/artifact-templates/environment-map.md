# Environment map

> Gate 0 deliverable. Fill from the repository and by actually starting the app — not from assumptions.
> Exit criterion: you can launch and reach the application. If not, stop and investigate.

## Execution environment
- OS / Python / Node:
- Selenium version:
- Browsers available (name → version):
- Docker / compose:
- CI/CD configuration found:

## Application
- Startup command(s):
- Required services (DB, cache, queue, storage):
- Required environment variables (names only, never values):
- Seed data / migrations:
- Base URL / API base URL:
- Authentication method and test accounts (role → where the credential lives):

## Existing tests and automation
- Test directories and frameworks found:
- What they cover / how to run them:

## Known limitations
- (e.g. external service unreachable, no test DB, browser not installed)

## Evidence
- Command run → result (startup log excerpt, health check response, screenshot):
